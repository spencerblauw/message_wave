name: message_wave
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  csv: ^6.0.0
  path_provider: ^2.0.11
  shared_preferences: ^2.0.15
  file_picker: ^8.0.5
  logging: ^1.0.2 
  permission_handler: ^11.3.1
  flutter_sms: 
    path: ../flutter_sms/flutter_sms/
    #git:
    #  url: https://github.com/fluttercommunity/flutter_sms.git
    #  ref: master
  url_launcher: ^6.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  

flutter:
  uses-material-design: true
  assets:
    - logo.png
